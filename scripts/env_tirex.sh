# TiRex (NX-AI/TiRex, xLSTM): conda env `tirex` (scripts/install_tirex.sh); the package's PyTorch sLSTM fallback, no custom kernels.
source "$(dirname "${BASH_SOURCE[0]}")/env_common.sh"
export PY="$CONDA_ENVS/tirex/bin/python"
